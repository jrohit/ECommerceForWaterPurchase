{
  "builds": [
    {
      "src": "./server.js",
      "use": "@vercel/node"
    }
  ],
  "buildCommand": "npm install && cd client && npm run build",
  "routes": [
    {
      "src": "/.*",
      "dest": "server.js"
    }
  ]
}
