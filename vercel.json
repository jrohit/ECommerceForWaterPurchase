{
  "builds": [
    {
      "src": "./server.js",
      "use": "@vercel/node"
    },
    { "src": "*.html", "use": "@vercel/static" }
  ],
  "buildCommand": "npm install && cd client && npm run build",
  "routes": [
    {
      "src": "/.*",
      "dest": "server.js"
    }
  ]
}
